


[] add the remaining dtabase function for all the use cases 20 main
[] updte the ice breaker (make it ask the question and save it. th cha will ask more question ountil it fills that in) update the rbains of the ai
[] make chat bot more strict 


1. ADD a function to check if anything exist in this table it siply returns truee
2. use it to chek if employee is new finalize unique id and implement in database first
3. create ice vreakers based on sheet and add them