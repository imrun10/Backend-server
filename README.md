# WhatsApp Bot

A simple WhatsApp bot project.

## Installation

1. Clone the repository.
2. Run the following command to install dependencies:

    ```
    npm install
    ```

## Development

Start the development server with:

```
npm run dev
```

## API Endpoints

Check the `index` file to find the links for each API endpoint  (which will be in their respective controller).
